import { PageLoader } from "@/components/ui/PageLoader";

/** Loading UI for authenticated routes (renders inside AppShell `<Main>`). */
export default function AppLoading() {
  return <PageLoader />;
}
